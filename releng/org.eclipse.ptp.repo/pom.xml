<?xml version="1.0" encoding="UTF-8"?>
<project
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd"
	xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>org.eclipse.ptp</groupId>
		<artifactId>ptp-parent</artifactId>
		<version>6.0.0-SNAPSHOT</version>
		<relativePath>../../pom.xml</relativePath>
	</parent>

	<version>6.0.0-SNAPSHOT</version>
	<artifactId>org.eclipse.ptp.repo</artifactId>
	<packaging>eclipse-repository</packaging>
	
	<profiles>
		<profile>
			<id>production</id>
			<properties>
				<ptp-install>/Users/greg/ptp-install</ptp-install>
			</properties>
			<build>
				<plugins>
					<plugin>
						<artifactId>maven-antrun-plugin</artifactId>
						<executions>
							<execution>
								<id>deploy</id>
								<phase>install</phase>
								<goals>
									<goal>run</goal>
								</goals>
								<configuration>
									<tasks>
										<mkdir dir="${ptp-install}"/>
										<delete>
											<fileset dir="${ptp-install}">
												<include name="**" />
											</fileset>
										</delete>
										<copy
											file="target/org.eclipse.ptp.repo.zip"
											tofile="${ptp-install}/org.eclipse.ptp.repo.${unqualifiedVersion}.${buildQualifier}.zip"/>
										<unzip
											src="target/org.eclipse.ptp.repo.zip"
											dest="${ptp-install}"/>
									</tasks>
								</configuration>
							</execution>
						</executions>
					</plugin>
				</plugins>
			</build>
		</profile>
	</profiles>
</project>
