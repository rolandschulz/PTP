// Generated by Ludwig version 1.0 alpha 6

package org.eclipse.photran.internal.core.parser;

import org.eclipse.photran.internal.core.lexer.Token;


/**
 * <ExprList> ::= Expr:<Expr> 
 * <ExprList> ::= @:<ExprList> tcomma:T_COMMA Expr:<Expr> 
 */
public class ASTExprListNode extends ParseTreeNode
{
    public ASTExprListNode(Nonterminal nonterminal, Production production)
    {
        super(nonterminal, production);
    }

    public int count()
    {
        ParseTreeNode node = this;
        int count = 1;
        while (node.getChild("@") != null)
        {
            count++;
            node = node.getChild("@");
        }
        return count;
    }

    public ASTExprNode getASTExpr(int index)
    {
        ASTExprListNode node = this;
        for (int i = 0; i < index; i++)
            node = (ASTExprListNode)node.getChild("@");
        return (ASTExprNode)node.getChild("Expr");
    }

    public Token getASTTcomma(int index)
    {
        ASTExprListNode node = this;
        for (int i = 0; i < index; i++)
            node = (ASTExprListNode)node.getChild("@");
        return node.getChildToken("tcomma");
    }

    protected void visitThisNodeUsing(ASTVisitor visitor) { visitor.visitASTExprListNode(this); }
}
