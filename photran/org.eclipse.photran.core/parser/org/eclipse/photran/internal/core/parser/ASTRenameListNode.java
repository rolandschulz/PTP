// Generated by Ludwig version 1.0 alpha 6

package org.eclipse.photran.internal.core.parser;

import org.eclipse.photran.internal.core.lexer.Token;


/**
 * <RenameList> ::= Rename:<Rename> 
 * <RenameList> ::= @:<RenameList> tcomma:T_COMMA Rename:<Rename> 
 */
public class ASTRenameListNode extends ParseTreeNode
{
    public ASTRenameListNode(Nonterminal nonterminal, Production production)
    {
        super(nonterminal, production);
    }

    public int count()
    {
        ParseTreeNode node = this;
        int count = 1;
        while (node.getChild("@") != null)
        {
            count++;
            node = node.getChild("@");
        }
        return count;
    }

    public ASTRenameNode getASTRename(int index)
    {
        ASTRenameListNode node = this;
        for (int i = 0; i < index; i++)
            node = (ASTRenameListNode)node.getChild("@");
        return (ASTRenameNode)node.getChild("Rename");
    }

    public Token getASTTcomma(int index)
    {
        ASTRenameListNode node = this;
        for (int i = 0; i < index; i++)
            node = (ASTRenameListNode)node.getChild("@");
        return node.getChildToken("tcomma");
    }

    protected void visitThisNodeUsing(ASTVisitor visitor) { visitor.visitASTRenameListNode(this); }
}
