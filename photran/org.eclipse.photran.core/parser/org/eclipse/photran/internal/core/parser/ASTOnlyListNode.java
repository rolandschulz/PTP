// Generated by Ludwig version 1.0 alpha 6

package org.eclipse.photran.internal.core.parser;

import org.eclipse.photran.internal.core.lexer.Token;


/**
 * <OnlyList> ::= Only:<Only> 
 * <OnlyList> ::= @:<OnlyList> tcomma:T_COMMA Only:<Only> 
 */
public class ASTOnlyListNode extends ParseTreeNode
{
    public ASTOnlyListNode(Nonterminal nonterminal, Production production)
    {
        super(nonterminal, production);
    }

    public int count()
    {
        ParseTreeNode node = this;
        int count = 1;
        while (node.getChild("@") != null)
        {
            count++;
            node = node.getChild("@");
        }
        return count;
    }

    public ASTOnlyNode getASTOnly(int index)
    {
        ASTOnlyListNode node = this;
        for (int i = 0; i < index; i++)
            node = (ASTOnlyListNode)node.getChild("@");
        return (ASTOnlyNode)node.getChild("Only");
    }

    public Token getASTTcomma(int index)
    {
        ASTOnlyListNode node = this;
        for (int i = 0; i < index; i++)
            node = (ASTOnlyListNode)node.getChild("@");
        return node.getChildToken("tcomma");
    }

    protected void visitThisNodeUsing(ASTVisitor visitor) { visitor.visitASTOnlyListNode(this); }
}
