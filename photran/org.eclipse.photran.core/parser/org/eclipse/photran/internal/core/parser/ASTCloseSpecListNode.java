// Generated by Ludwig version 1.0 alpha 6

package org.eclipse.photran.internal.core.parser; import org.eclipse.photran.internal.core.lexer.*;


/**
 * <CloseSpecList> ::= UnitIdentifier:<UnitIdentifier>  :production684
 * <CloseSpecList> ::= CloseSpec:<CloseSpec>  :production685
 * <CloseSpecList> ::= @:<CloseSpecList> tcomma:T_COMMA CloseSpec:<CloseSpec>  :production686
 */
public class ASTCloseSpecListNode extends ParseTreeNode
{
    public ASTCloseSpecListNode(Nonterminal nonterminal, Production production)
    {
        super(nonterminal, production);
    }

    public int count()
    {
        ParseTreeNode node = this;
        int count = 1;
        while (node.getChild("@") != null)
        {
            count++;
            node = node.getChild("@");
        }
        return count;
    }

    public ASTUnitIdentifierNode getASTUnitIdentifier(int index)
    {
        ASTCloseSpecListNode node = this;
        for (int i = 0; i < index; i++)
            node = (ASTCloseSpecListNode)node.getChild("@");
        return (ASTUnitIdentifierNode)node.getChild("UnitIdentifier");
    }

    public ASTCloseSpecNode getASTCloseSpec(int index)
    {
        ASTCloseSpecListNode node = this;
        for (int i = 0; i < index; i++)
            node = (ASTCloseSpecListNode)node.getChild("@");
        return (ASTCloseSpecNode)node.getChild("CloseSpec");
    }

    public Token getASTTcomma(int index)
    {
        ASTCloseSpecListNode node = this;
        for (int i = 0; i < index; i++)
            node = (ASTCloseSpecListNode)node.getChild("@");
        return node.getChildToken("tcomma");
    }

    protected void visitThisNodeUsing(ASTVisitor visitor) { visitor.visitASTCloseSpecListNode(this); }
}
